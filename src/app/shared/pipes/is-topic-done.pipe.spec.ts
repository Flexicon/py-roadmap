import { IsTopicDonePipe } from './is-topic-done.pipe';

describe('IsTopicDonePipe', () => {
  it('create an instance', () => {
    const pipe = new IsTopicDonePipe();
    expect(pipe).toBeTruthy();
  });
});
